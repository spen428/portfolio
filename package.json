{
  "name": "curriculum-vitae",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm run -r build",
    "build:prod": "pnpm run -r build:prod",
    "run:prod": "pnpm run -r prod",
    "run:dev": "pnpm run -r dev",
    "lint": "pnpm run -r lint",
    "test": "pnpm run -r test"
  },
  "devDependencies": {
    "backstopjs": "6.3.3"
  }
}
